import React from "react";
import {View, Text, StyleSheet} from "react-native";

const PlanningScreen = () => {
    return <Text>this is the planning screen</Text>
};

const styles = StyleSheet.create({});

export default PlanningScreen;